<?xml version="1.0"?>
<psalm
    errorLevel="1"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
>
    <projectFiles>
        <directory name="bin" /><!-- #todo remove this folder in library -->
        <directory name="config" /><!-- #todo remove this folder in library -->
        <directory name="data" /><!-- #todo remove this folder in library -->
        <directory name="public" /><!-- #todo remove this folder in library -->
        <directory name="src" />
        <directory name="tests" />
        <directory name="view" /><!-- #todo remove this folder in library -->
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>

    <issueHandlers>
        <PropertyNotSetInConstructor>
            <errorLevel type="suppress">
                <directory name="tests" />
            </errorLevel>
        </PropertyNotSetInConstructor>
    </issueHandlers>
</psalm>
